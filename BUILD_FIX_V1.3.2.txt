Healthome V1.3.2 build fix

Fixed the EAS/Expo config error:
- expo-status-bar is a runtime dependency, not a config plugin.
- Removed expo-status-bar from expo.plugins in app.json.
- Kept expo-status-bar ~3.0.9 in dependencies because App.js imports StatusBar from it.
- Bumped app version to 1.3.2 and Android versionCode to 7.
- Kept the APK EAS build profile.

Recommended validation:
npm install
npx expo export --platform android
npx eas-cli@latest build -p android --profile apk --clear-cache
